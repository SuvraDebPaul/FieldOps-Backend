/** Bangladesh standard VAT rate applied to labour + parts. */
export const VAT_RATE = 0.15;

/** Days from issue to due date. */
export const INVOICE_DUE_DAYS = 7;

/** Minimum billable labour, so a five-minute job is not billed as zero. */
export const MINIMUM_BILLABLE_HOURS = 0.5;
